#include <stdio.h>

int main()
{
	// 사용자에게 2개의 정수를 입력 받는 코드
	// 핵심: 2개의 정수를 입력받으려면 2개의 변수 필요
	int n1 = 0, n2 = 0;
	scanf_s("%d %d", &n1, &n2);

	// 사용자에게 5개의 정수를 입력 받는 코드
	int a, b, c, d, e;
	for (int i = 0; i < 5; ++i)
	{
		scanf_s("%d", ??); // ??를 어떻게 해야 할 까?
							// 해결책은 다음 소스
	}

	return 0;
}