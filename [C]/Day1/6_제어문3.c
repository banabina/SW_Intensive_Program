#include <stdio.h>
// 38 page
int main()
{
	int score = 70;
	if (score > 60) // 61 부터 true
	{
		printf("성인\n");
	}
	else if (score <= 50 && score <= 60)
	{

	}
	int n = 4;
	if (score > 50 || ++n > 5) { // 앞이 참이므로 뒤는 실행안됨.


	}
}